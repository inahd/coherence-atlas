# Pattern Lattice

Atlas currently models a transformation chain such as:

`śabda -> mantra -> swara -> rhythm -> number -> geometry -> yantra -> mandala -> temple layout`

This is both:

- a research pathway
- a graph pathway
- a future generator pathway
